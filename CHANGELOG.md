# PartsBridge AD v0.3.8

- PCB 焊盘恢复工具不再通过第三方解析器重写整份 PcbDoc，只在原生 OLE 容器中替换等长的 `Pads6/Data`；模型、3D Body、网络、走线及其他数据流保持逐字节不变。
- 写出前后强制核对 `Models` / `ModelsNoEmbed` 数量头与实际记录，拒绝模型表失配、焊盘流长度变化、非目标流变化和悬空 3D 模型引用。
- 在 AD 26.9.1 中完成真实故障板恢复后重新打开和原生保存；保存后的模型表为 54/54，230 个 3D Body 无悬空引用。仓库和发布包不包含该板、元件库、模型或个人路径。
- 合并此前尚未公开发布的 0.3.6 中国站优先搜索和 0.3.7 STEP 最低点归一化；共 142 项测试通过。
- Windows EXE 文件属性中的 `CompanyName`、版权和产品版本为 `foke` / `0.3.8.0`；本版本仍未做 Authenticode 签名。

## PartsBridge AD v0.3.7

- 修复 STEP 3D 模型约一半埋入 PCB 的坐标基准错误：不再把立创 Z 元数据直接叠加到 STEP 已有坐标，而是先读取旋转后 STEP 的真实最低点，再将其映射到立创定义的目标最低高度。
- 贴片模型的最低实体面自动落到板面；插件模型保留立创给出的负 Z 针脚深度，因此针脚仍可穿板，主体不会被重复下沉。无法解析 STEP 边界时保守沿用原偏移，并在维护清单记录明确警告。
- 使用 0805、DIP、电源模块及 2.54 mm 插件连接器共 7 个真实 STEP 做原生 PcbLib 写入—回读：底面、顶面和模型 Z 偏移全部匹配计算结果；139 项测试通过。
- Windows EXE 文件属性中的 `CompanyName`、版权和产品版本为 `foke` / `0.3.7.0`；本版本仍未做 Authenticode 签名。

## PartsBridge AD v0.3.6

- 搜索与 C 编号详情优先使用立创 EDA 中国站公开数据，再在中国站无结果或请求失败时回退原有全球公开接口，解决“中国站有货、软件却搜不到”的数据分区问题。
- 中国站结果保留 C 编号、制造商料号、品牌、封装、国内库存、人民币阶梯首价、数据手册和中国商城商品页；“仅有库存”按中国站库存过滤。
- 使用当前在线数据实测 `TD331SCANH` 与 `C7527764` 均精确返回同一元件，库存 2、封装 SMD-9；在自动清理的隔离目录实际生成 SchLib/PcbLib 并嵌入 STEP 3D，原生回读、哈希和符号—封装链接验证通过。
- 新增三项中国站优先、C 编号详情和缺货过滤回归，共 134 项测试通过。
- Windows EXE 文件属性中的 `CompanyName`、版权和产品版本为 `foke` / `0.3.6.0`；本版本仍未做 Authenticode 签名。

## PartsBridge AD v0.3.5

- 新增写库前 AD 权限预检：非 Windows 或 AD 未运行时允许离线追加；AD 正在运行时必须只有一个同会话实例，且与元件库桥权限一致，否则在下载和创建总库目录前停止。
- 权限预检只读取进程信息，不启动、关闭或操控 AD，不请求 UAC，不自动提权；手动关闭“追加后刷新”也不能绕过写库前门控。
- 现有总库出现本地符号—封装链接失配时，只有上次发布清单的 SHA-256/大小能证明文件后来变化，才指出被改写的 SchLib/PcbLib；只有备份实际包含完整库对时才给出成对恢复位置。
- 当前数据下按真实顺序生成 C32713268、C192062（AD5422BREZ-REEL）并嵌入 3D，连续追加、合并、再次读取均通过；未复现器件自身导致的链接失配。
- 项目元数据、总库维护清单和发布说明将维护/发布者记录为 `foke`。
- Windows EXE 文件属性中的 `CompanyName`、版权和产品版本已写入 `foke` / `0.3.5.0`；本版本未做 Authenticode 签名，因此 Windows 安全提示中的“已验证发布者”仍不会由该元数据替代。
- 新增六项权限与库对诊断回归，共 131 项测试通过；同权限 AD 实机追加与缓存刷新仍需使用者验收。

## PartsBridge AD v0.3.4

- 修正独立 `.PcbLib` 的封装回读：改用 PCB library iterator 读取全部封装名和数量，不再误用原理图组件计数接口。
- 已打开的目标库只复用、不关闭。未加载的目标 PcbLib通过 AD 接口临时打开用于回读，只有本次打开且仍未修改的文档才关闭；不调用显示、保存、卸载或重装库命令。
- 回执分别报告符号与封装的数量/完整名称匹配状态；数量相等但名称未匹配或缺少校验标志时仍不能报告成功。
- 警告显示实际/期望数量，将“未回读”与真实零条目区分，不再笼统归因于未安装库。只刷新无需重新下载或追加。
- 新增六项回归，修复前失败、修复后共 125 项测试通过；AD PCB 枚举及临时文档行为仍待同权限实机验收。

## PartsBridge AD v0.3.3

- 修复刷新脚本报 `Can't access top level variable`：不再让嵌套辅助函数访问外层局部变量，改为独立函数并显式传入本次回执对象。
- 原有未保存编辑保护、过期检查、事务门控和回执校验保持不变；不重新下载或改写库。
- 新增两项脚本作用域回归：旧写法失败，修复后共 119 项测试通过。Python 测试不等于 AD 脚本引擎及面板验收。
- 遇到旧脚本的 `Continue execution?` 提示时选择 No，停止旧脚本；完整解压新版，用与 AD 相同的权限运行，再点击“刷新 AD 库”。

## PartsBridge AD v0.3.2

- 新增追加成功后自动刷新 AD 库，以及独立的“刷新 AD 库”按钮和 `refresh-ad` 命令。
- 只对真正发布的新增元件自动触发；重复跳过、失败、取消不会触发。刷新失败不改变库发布结果。
- 通过 AD 原生脚本接口重载未修改的目标库，刷新已安装库与 Components 缓存；不保存、关闭工程或删除库。
- 保护未保存编辑，检测同权限/多会话/超时；只有 AD 回执及条目清单匹配才报告成功。
- 本机 AD 26.9.1 命令注册和权限保护已核实；当前权限差异阻止了真实脚本回执及面板显示验收，详见验证记录。

## PartsBridge AD v0.3.1

元件库桥：面向立创/LCSC 元件检索的 Windows 本地工具，将确认后的 C 编号追加到长期维护的 Altium 原生库。

## 本版本

- 搜索和批量查询候选；由使用者核对型号后加入队列。
- 输出一套 `LCSC.SchLib` / `LCSC.PcbLib`，保留历史符号、引脚、封装及内嵌 STEP；不写入原理图。
- 已有 C 编号提前跳过；新增元件在暂存目录验证，发布前备份，支持受保护的中断恢复。
- 修复非零 3D 高度偏移或旋转导致的 `native item preservation failed`；完整性校验仍然开启。
- Windows 发布包包含依赖许可证和来源说明；不包含个人元件库、BOM、缓存、账号凭据或模型文件。

## 下载与使用

- `PartsBridge-AD-v0.3.1-Windows-x64.zip`：完整解压后运行 `PartsBridge-AD.exe`；不要只复制 EXE。
- `PartsBridge-AD-v0.3.1-source.zip`：应用源码、测试和构建配置。
- `SHA256SUMS.txt`：下载文件的 SHA-256 校验和。

首次启动请选择自己电脑上存在且可写的总库目录。0.3.1 在没有已保存偏好时的默认目录使用 G 盘；没有 G 盘时必须改选。

追加前在 Altium 中保存并关闭两份库。失败批次的 STEP 没有持久缓存，重试可能重新请求；已有元件不会自动更新或补模型。

## 验证与边界

82 项单元测试通过；原生库副本连续追加和重复跳过验证通过，原有符号、封装和 STEP 保持一致。Windows 包通过离线自检及原生库验真。

这些结果不等于 Altium GUI 可视检查或工程签核；投板前必须对照数据手册复核。公开 JSON 数据源可能限流或变化，库存/价格不保证等同中国站实时成交信息。详细边界见 `VALIDATION_REPORT.md` 和 `README.md`。

本项目采用 AGPL-3.0-or-later；第三方组件遵循各自许可证。本项目与立创、嘉立创、EasyEDA、Altium 无隶属或背书关系。
